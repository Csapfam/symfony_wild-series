{% extends "base.html.twig" %}

{% block body %}

    {{ form_start(form) }}
    <div class="form">
    {{ form_widget(form) }}
    <input type='submit' value='Add program'>
    </div>
    {{ form_end(form) }}
{% endblock %}